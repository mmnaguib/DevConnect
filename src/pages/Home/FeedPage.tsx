import { useDispatch } from "react-redux";
import { logout } from "../../store/authSlice";

export default function FeedPage() {
  return (
    <div>
      <h1>Feed Page - Posts & Updates</h1>
    </div>
  );
}
